name: Agendador de amor

on:
  schedule:
    - cron: '0 */2 * * *'

jobs:
  executar-amor:
    runs-on: ubuntu-latest
    steps:
    - name: Checar código
      uses: actions/checkout@v2
    - name: Executar script
      run: python amor.py
